import { Metadata } from "next"
export const metadata:Metadata={
  title:{
    absolute:"Ignore main layout template title"
  },

};
const page = () => {
  return (
    <div>page</div>
  )
}

export default page